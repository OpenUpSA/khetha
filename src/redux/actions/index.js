const mergeStateFromRemote = () => true;
const overrideRemoteState = () => true;
const replaceEntireState = () => true;


export {
  mergeStateFromRemote,
  overrideRemoteState,
  replaceEntireState,
};

export default {
  mergeStateFromRemote,
  overrideRemoteState,
  replaceEntireState,
};
