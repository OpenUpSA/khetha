---
name: Icon
menu: Components
---


import { Playground, PropsTable } from 'docz'
import Icon, { list } from './index.jsx';


# Icon

![stability-experimental](https://img.shields.io/badge/stability-experimental-orange.svg)

**...**

Looking for a specific file?

- [index.jsx](#indexjsx)

## `index.jsx`

### Props

<PropsTable of={Icon} />

### Demos

#### **Basic**

<Playground>
  <Icon type="Security" />
</Playground>

#### **Sizes**

<Playground>
  <Icon type="LocalShipping" size="small" />
  <Icon type="LocalShipping" size="standard" />
  <Icon type="LocalShipping" size="large" />
  <Icon type="LocalShipping" size="huge" />
  <Icon type="LocalShipping" size="massive" />
</Playground>

#### **Types**

<Playground>
    {
      () => {
        const all = list.map(type => (
          <div 
            key={type}
            style={{ display: 'flex', alignItems: 'center' }}
          >
            <Icon size="huge" {...{ type }} />
            <span style={{ paddingLeft: '15px' }}>{type}</span>
          </div>
        ))
        return (
          <div>
            {all}
          </div>
        );
      }
    }
</Playground>
